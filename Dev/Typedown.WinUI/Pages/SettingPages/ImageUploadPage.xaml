<Page
    x:Class="Typedown.WinUI.Pages.SettingPages.ImageUploadPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Typedown.WinUI.Pages.SettingPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Typedown.WinUI.Controls"
    xmlns:enums="using:Typedown.Core.Enums"
    xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
    xmlns:u="using:Typedown.WinUI.Controls"
    xmlns:models="using:Typedown.Core.Models"
    Unloaded="OnUnloaded"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Page.Resources>
        <SolidColorBrush x:Key="SystemFillColorCriticalBrush" Color="{ThemeResource SystemFillColorCritical}"/>
        <MenuFlyout x:Key="ConfigItemMenuFlyout" x:Name="ConfigItemMenuFlyout">
            <MenuFlyoutItem Text="{u:LocaleString Key=Delete}" Click="OnDeleteClick"/>
        </MenuFlyout>
    </Page.Resources>

    <StackPanel Style="{StaticResource SettingsPageRootStackPanelStyle}">
        <toolkit:SettingsCard Header="{u:LocaleString Key=ImageUpload.AddConfig.Title}" IsActionIconVisible="False">
            <toolkit:SettingsCard.HeaderIcon>
                <FontIcon Glyph="&#xee71;"/>
            </toolkit:SettingsCard.HeaderIcon>
            <Button Style="{ThemeResource AccentButtonStyle}" MinWidth="100" Click="OnAddButtonClick">
                <TextBlock Text="{u:LocaleString Key=Add}"/>
            </Button>
        </toolkit:SettingsCard>

        <TextBlock Text="{u:LocaleString Key=ImageUpload.Configs}" Style="{StaticResource SettingsSectionHeaderTextBlockStyle}" Margin="{StaticResource SettingsSectionBreakMargin}" Visibility="{x:Bind ConfigItemsTitleVisibility(ImageUpload.ImageUploadConfigs), Mode=OneWay}"/>
        <ListView
            x:Name="ConfigListView"
            Grid.Row="1"
            Grid.ColumnSpan="4"
            ItemsSource="{x:Bind ImageUpload.ImageUploadConfigs}"
            ItemContainerStyle="{StaticResource SettingsListViewItemStyle}"
            SelectionMode="None">
            <ListView.ItemContainerTransitions>
                <TransitionCollection>
                    <AddDeleteThemeTransition/>
                    <ContentThemeTransition/>
                    <ReorderThemeTransition/>
                </TransitionCollection>
            </ListView.ItemContainerTransitions>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:ImageUploadConfig">
                    <toolkit:SettingsCard
                        Header="{x:Bind Name, Mode=OneWay}"
                        Description="{x:Bind local:ImageUploadPage.GetConfigItemDescription(Method, IsEnable), Mode=OneWay}"
                        Tag="{x:Bind}"
                        IsClickEnabled="True"
                        Click="{x:Bind local:ImageUploadPage.OnConfigItemClick}"
                        ContextFlyout="{StaticResource ConfigItemMenuFlyout}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </StackPanel>
</Page>
