<MenuFlyoutItem
    x:Class="Typedown.WinUI.Controls.ContextFormatItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Typedown.WinUI.Controls"
    xmlns:controls="using:Typedown.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:Typedown.Core.ViewModels"
    mc:Ignorable="d">
    <MenuFlyoutItem.Template>
        <ControlTemplate TargetType="local:ContextFormatItem">
            <StackPanel Orientation="Horizontal" Spacing="4" Padding="4,2,4,2" d:DataContext="{d:DesignInstance Type=viewmodels:AppViewModel}">
                <Button Style="{StaticResource EllipsisIconButtonStyle}" Command="{TemplateBinding FormatCommand}" CommandParameter="strong">
                    <FontIcon FontSize="16" Glyph="&#xE8DD;"/>
                </Button>
                <Button Style="{StaticResource EllipsisIconButtonStyle}" Command="{TemplateBinding FormatCommand}" CommandParameter="em">
                    <FontIcon FontSize="16" Glyph="&#xE8DB;"/>
                </Button>
                <Button Style="{StaticResource EllipsisIconButtonStyle}" Command="{TemplateBinding FormatCommand}" CommandParameter="u">
                    <FontIcon FontSize="16" Glyph="&#xE8DC;"/>
                </Button>
                <Button Style="{StaticResource EllipsisIconButtonStyle}" Command="{TemplateBinding FormatCommand}" CommandParameter="del">
                    <FontIcon FontSize="16" Glyph="&#xEDE0;"/>
                </Button>
                <Button Style="{StaticResource EllipsisIconButtonStyle}" Command="{TemplateBinding FormatCommand}" CommandParameter="clear">
                    <FontIcon FontSize="16" Glyph="&#xe75c;"/>
                </Button>
            </StackPanel>
        </ControlTemplate>
    </MenuFlyoutItem.Template>
</MenuFlyoutItem>
