<local:MenuBarItemBase
    Title="{u:LocaleString Key=View}"
    x:Class="Typedown.WinUI.Controls.ViewItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Typedown.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:u="using:Typedown.WinUI.Controls"
    Unloaded="OnUnloaded"
    mc:Ignorable="d">
    <ToggleMenuFlyoutItem x:Name="SidePaneItem" Text="{u:LocaleString Key=SidePane}" IsChecked="False"/>
    <MenuFlyoutSeparator/>
    <ToggleMenuFlyoutItem x:Name="SourceCodeModeItem" Text="{u:LocaleString Key=SourceCodeMode}" IsChecked="False"/>
    <ToggleMenuFlyoutItem x:Name="FocusModeItem" Text="{u:LocaleString Key=FocusMode}" IsChecked="False"/>
    <ToggleMenuFlyoutItem x:Name="TypewriterModeItem" Text="{u:LocaleString Key=TypewriterMode}" IsChecked="False"/>
    <MenuFlyoutSeparator/>
    <ToggleMenuFlyoutItem x:Name="StatusBarItem" Text="{u:LocaleString Key=StatusBar}" IsChecked="False"/>
</local:MenuBarItemBase>
