<Flyout
    x:Class="Typedown.WinUI.Controls.ImageToolbar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Typedown.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:u="using:Typedown.WinUI.Controls"
    Opened="OnOpened"
    Closed="OnClosed"
    ShowMode="Transient"
    Placement="TopEdgeAlignedLeft"
    mc:Ignorable="d">
    <Flyout.FlyoutPresenterStyle>
        <Style TargetType="FlyoutPresenter" BasedOn="{StaticResource DefaultFlyoutPresenterStyle}" >
            <Setter Property="Padding" Value="4" />
        </Style>
    </Flyout.FlyoutPresenterStyle>
    <Grid>
        <StackPanel Orientation="Horizontal">
            <Button Grid.Column="2" Width="32" Height="32" Style="{ThemeResource EllipsisIconButtonStyle}" Click="EditClick" ToolTipService.ToolTip="{u:LocaleString Key=Edit}">
                <FontIcon FontSize="14" Glyph="&#xE70F;"/>
            </Button>
            <AppBarSeparator/>
            <Button Grid.Column="2" Width="32" Height="32" Style="{ThemeResource EllipsisIconButtonStyle}" Click="InlineClick" ToolTipService.ToolTip="{u:LocaleString Key=Inline}">
                <FontIcon FontSize="14" Glyph="&#xe8a1;"/>
            </Button>
            <Button Grid.Column="2" Width="32" Height="32" Style="{ThemeResource EllipsisIconButtonStyle}" Click="LeftClick" ToolTipService.ToolTip="{u:LocaleString Key=AlignLeft}">
                <FontIcon FontSize="14" Glyph="&#xE8E4;"/>
            </Button>
            <Button Grid.Column="2" Width="32" Height="32" Style="{ThemeResource EllipsisIconButtonStyle}" Click="CenterClick" ToolTipService.ToolTip="{u:LocaleString Key=AlignCenter}">
                <FontIcon FontSize="14" Glyph="&#xE8E3;"/>
            </Button>
            <Button Grid.Column="2" Width="32" Height="32" Style="{ThemeResource EllipsisIconButtonStyle}" Click="RightClick" ToolTipService.ToolTip="{u:LocaleString Key=AlignRight}">
                <FontIcon FontSize="14" Glyph="&#xE8E2;"/>
            </Button>
            <AppBarSeparator/>
            <Button Grid.Column="3" Width="32" Height="32" Style="{ThemeResource EllipsisIconButtonStyle}" ToolTipService.ToolTip="{u:LocaleString Key=Zoom}">
                <FontIcon FontSize="14" Glyph="&#xe9a6;"/>
                <Button.Flyout>
                    <MenuFlyout Placement="Top">
                        <MenuFlyoutItem Text="25%" Tag="25%" Click="ZoomClick"/>
                        <MenuFlyoutItem Text="33%" Tag="33%" Click="ZoomClick"/>
                        <MenuFlyoutItem Text="50%" Tag="50%" Click="ZoomClick"/>
                        <MenuFlyoutItem Text="67%" Tag="67%" Click="ZoomClick"/>
                        <MenuFlyoutItem Text="80%" Tag="80%" Click="ZoomClick"/>
                        <MenuFlyoutSeparator/>
                        <MenuFlyoutItem Text="100%" Tag="100%" Click="ZoomClick"/>
                        <MenuFlyoutItem Text="150%" Tag="150%" Click="ZoomClick"/>
                        <MenuFlyoutSeparator/>
                        <MenuFlyoutItem Text="200%" Tag="200%" Click="ZoomClick"/>
                    </MenuFlyout>
                </Button.Flyout>
            </Button>
            <AppBarSeparator/>
            <Button Grid.Column="2" Width="32" Height="32" Style="{ThemeResource EllipsisIconButtonStyle}" Click="DeleteClick" ToolTipService.ToolTip="{u:LocaleString Key=Delete}">
                <FontIcon FontSize="14" Glyph="&#xE74D;"/>
            </Button>
        </StackPanel>
    </Grid>
</Flyout>
