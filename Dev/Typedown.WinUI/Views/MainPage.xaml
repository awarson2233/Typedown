<?xml version="1.0" encoding="UTF-8" ?>
<Page
    x:Class="Typedown.WinUI.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Typedown.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Typedown.WinUI.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:Typedown.UI.ViewModels"
    mc:Ignorable="d">
    <Grid Background="#F4EFE5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Padding="40,36,40,20" Spacing="10">
            <TextBlock
                FontSize="34"
                FontWeight="SemiBold"
                Foreground="#19363B"
                Text="{x:Bind ViewModel.Title, Mode=OneWay}" />
            <TextBlock
                FontSize="16"
                Foreground="#4B5B5E"
                Text="{x:Bind ViewModel.Subtitle, Mode=OneWay}" />
            <TextBlock
                FontSize="14"
                Foreground="#6A7A7D"
                Text="{x:Bind ViewModel.ContractsProbeSummary, Mode=OneWay}" />
        </StackPanel>

        <Grid Grid.Row="1" Padding="40,10,40,40" ColumnSpacing="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border
                Background="White"
                BorderBrush="#D8CBB8"
                BorderThickness="1"
                CornerRadius="18"
                Padding="24">
                <Grid RowSpacing="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock
                        FontSize="22"
                        FontWeight="SemiBold"
                        Foreground="#19363B"
                        Text="Phase 11 editable editor smoke" />
                    <controls:WinUIEditorHost Grid.Row="1" />
                </Grid>
            </Border>

            <Border
                Grid.Column="1"
                Background="#19363B"
                CornerRadius="18"
                Padding="24">
                <StackPanel Spacing="16">
                    <TextBlock
                        FontSize="22"
                        FontWeight="SemiBold"
                        Foreground="White"
                        Text="Migration boundary" />
                    <ItemsRepeater ItemsSource="{x:Bind ViewModel.ValidatedItems, Mode=OneWay}">
                        <ItemsRepeater.ItemTemplate>
                            <DataTemplate x:DataType="ui:MigrationBoundaryItem">
                                <TextBlock
                                    Margin="0,0,0,8"
                                    FontSize="15"
                                    Foreground="#F4EFE5"
                                    Text="{x:Bind Text}" />
                            </DataTemplate>
                        </ItemsRepeater.ItemTemplate>
                    </ItemsRepeater>
                    <ItemsRepeater ItemsSource="{x:Bind ViewModel.ServiceItems, Mode=OneWay}">
                        <ItemsRepeater.ItemTemplate>
                            <DataTemplate x:DataType="ui:MigrationBoundaryItem">
                                <TextBlock
                                    Margin="0,0,0,8"
                                    FontSize="15"
                                    Foreground="#F4EFE5"
                                    Text="{x:Bind Text}" />
                            </DataTemplate>
                        </ItemsRepeater.ItemTemplate>
                    </ItemsRepeater>
                    <ItemsRepeater ItemsSource="{x:Bind ViewModel.DeferredItems, Mode=OneWay}">
                        <ItemsRepeater.ItemTemplate>
                            <DataTemplate x:DataType="ui:MigrationBoundaryItem">
                                <TextBlock
                                    Margin="0,0,0,8"
                                    FontSize="15"
                                    Foreground="#F4EFE5"
                                    Text="{x:Bind Text}" />
                            </DataTemplate>
                        </ItemsRepeater.ItemTemplate>
                    </ItemsRepeater>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
